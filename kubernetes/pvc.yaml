apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Omitting storageClassName lets GKE automatically use its default standard/gce-pd storage class,
  # ensuring seamless cross-cloud compatibility!
